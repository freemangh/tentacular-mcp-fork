# Tasks

## Implementation

- [ ] Rewrite README.md Installation section with Helm as primary method
- [ ] Remove all references to `tntc cluster install` from README.md
- [ ] Document key Helm values (image, namespace, moduleProxy, auth)
- [ ] Add Post-Install section for CLI configuration
- [ ] Update Quick Start to follow Helm-first flow
- [ ] Review and update any other docs that reference `tntc cluster install`

## Verification

- [ ] No references to `tntc cluster install` remain in any documentation
- [ ] Helm install command is copy-pasteable and correct
- [ ] Post-install CLI configuration instructions are accurate
