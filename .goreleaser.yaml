version: 2

project_name: tentacular-mcp

before:
  hooks:
    - go mod tidy

snapshot:
  version_template: "{{ incpatch .Version }}-next"

gomod:
  proxy: true

builds:
  - id: tentacular-mcp
    main: ./cmd/tentacular-mcp
    binary: tentacular-mcp
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X github.com/randybias/tentacular-mcp/pkg/version.Version={{.Tag}}
      - -X github.com/randybias/tentacular-mcp/pkg/version.Commit={{.Commit}}
      - -X github.com/randybias/tentacular-mcp/pkg/version.Date={{.CommitDate}}

archives:
  - format: binary
    name_template: "tentacular-mcp_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

release:
  github:
    owner: randybias
    name: tentacular-mcp
  draft: false
  prerelease: auto
  name_template: "{{.Tag}}"

changelog:
  sort: asc
  use: github
  format: "{{ .SHA }}: {{ .Message }}"
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "Merge pull request"
      - "Merge branch"
  groups:
    - title: "New Features"
      regexp: '^.*?feat(\(.+\))??!?:.+$'
      order: 0
    - title: "Bug Fixes"
      regexp: '^.*?fix(\(.+\))??!?:.+$'
      order: 1
    - title: "Other"
      order: 999
